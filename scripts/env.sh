#!/usr/bin/env bash
# Source this before running cargo / stellar commands so they're on PATH
# in the current (stale-PATH) shell session:  source scripts/env.sh
export PATH="/c/Users/huawei/.cargo/bin:/c/Program Files (x86)/Stellar CLI:$PATH"
