# Kolektibo — Production Build Execution Log
_Started: 2026-07-11 05:52 MPST · tracks the production-roadmap implementation_

Running log so progress survives context resets. Plan: [production-roadmap](./production-roadmap_2026-07-11_0552.md). Working agreement: commit per milestone (conventional commits, no attribution trailer), keep `pnpm build` + `cargo test` green, re-verify the demo loop after each milestone, **migrations are approval-gated**.

## Legend
✅ done · 🔨 in progress · ⏳ next · ⛔ blocked/parked (external dep)

---

## Milestone A — Security + groundwork

- ✅ **P0 secret hygiene.** Full git-history scan (all refs) → **clean** (no secret/`.env` ever committed). Parameterized `.mcp.json` supabase block to `${SUPABASE_ACCESS_TOKEN}` / `${SUPABASE_PROJECT_REF}` (working copy is now token-free). Local secret files gitignored. Migration-approval saved to memory.
  - ⛔ **Owner action:** rotate the exposed token `sbp_7120…` in the Supabase dashboard; set the two env vars when a real project exists.
- ✅ **Roadmap doc** `docs/production-roadmap_2026-07-11_0552.md`; linked from `docs/00-INDEX` + `ARCHITECTURE.md`.
- ✅ **Supabase scaffold** `supabase/`: `migrations/0001_init.sql` (19 tables, 26 RLS policies, 7 fns incl. `is_pool_member`/`is_pool_officer`/`shares_pool`, `preview_pool`/`redeem_invite` RPCs, RLS test matrix), `seed.sql`, `config.toml`, `README.md`, `.gitignore`. Static checks pass (RLS on every table).
  - ⏳ **Approval-gated:** validate `0001_init.sql` by `supabase start` + `supabase db reset` on a local ephemeral DB (CLI 2.20.12 + Docker present). Not run yet — awaiting go-ahead (it's a migration apply).

## Milestone B — Phase 1 (auth · profile · membership · indexer) — ⏳ next

- ⏳ Supabase client + auth (magic-link/Google/anon→link); profile CRUD + settings; i18n framework (en/tl).
- ⏳ Backend v1 (stellar-sdk server, nonce challenge, notification fan-out); pools/membership (invite RPCs); indexer v0 poller → `chain_events`; Web Push plumbing.
- Note: much of B is code-complete-able now; live auth needs the real Supabase project (⛔ owner creates).

## Milestone C — contract v2 + paluwagan + money-UX — ⏳
## Milestone D — passkeys + hardening — ⏳

---

## Key decisions log
- Money authority on-chain only; Supabase = metadata (schema v1 keyed by chain ids; indexer/service-role writes chain rows).
- Auth launch = email + Google (+ anon); phone OTP → Phase 5.
- Custody = passkeys via **OZ Smart Accounts / smart-account-kit** (NOT legacy passkey-kit); relayer = **OZ Relayer/Channels** (Launchtube archived).
- Paluwagan = core; strict "no returns/invest" regulatory copy.
- v1 contract has no upgrade entrypoint → v1→v2 = redeploy + officer-approved fund move.
- Migrations approval-gated; Supabase MCP stays read-only.
